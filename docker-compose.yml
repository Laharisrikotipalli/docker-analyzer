version: "3.8"
services:
  analyzer:
    build: ./analyzer_tool
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  dashboard:
    build: ./dashboard
    ports:
      - "3000:80"